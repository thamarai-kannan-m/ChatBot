const config = {
    botName: 'Chatbot',
    initialMessages: [
        { text: 'Hello! How can I assist you today?', user: 'bot' }
    ],
    inputPlaceholder: 'Type a message...',
};

export default config;
